
---

# Отчёт об исправлениях кода  
## (Code Review + Google Java Style + Исправления ошибок)


---

# 1. Общие исправления по проекту

- Приведение всех имён переменных к стилю camelCase.  
- Приведение имён методов к стилю camelCase.  
- Удаление односимвольных переменных (`A`, `B`, `f1`, `f2`).  
- Удаление лишних пустых строк.  
- Исправление комментариев.  
- Замена конкатенации строк в циклах на StringBuilder.  
- Замена сообщений об ошибках на исключения.  
- Инкапсуляция приватных полей.  
- Приведение вывода в консоль к единообразному стилю.  

---

# 2. Main.java

1. Переименование переменных:
   - CleopatraName → cleopatraName  
   - A, B, C → cityA, cityB, cityC  
   - f1, f2, f3 → fraction1, fraction2, fraction3  

2. Улучшение читаемости:
   - Добавлены поясняющие строки вывода.  
   - Упорядочена логика создания объектов.  

---

# 3. Point.java

1. Поля оставлены приватными.  
2. Геттеры и сеттеры оформлены корректно.  
3. Метод toString() возвращает строку вида `{ x;y }`.  

(Функциональных ошибок не было.)

---

# 4. Fraction.java

1. Обработка ошибок переведена на исключения:
   - Проверка знаменателя в конструкторе.  
   - Проверка деления на ноль в методах div(Fraction) и div(int).  

2. Упрощение дроби:
   - Метод simplify() нормализует знак.  
   - Метод gcd() корректно работает с отрицательными числами.  

3. Мелкие исправления:
   - Удалена лишняя пустая строка в sum(int).  

---

# 5. ForPoint.java

1. Инкапсуляция:
   - Поле scanner сделано приватным.  

2. Именование:
   - Метод Coordinate() переименован в coordinate().  

3. Исправление ошибки ввода:
   - Удалён лишний вызов scanner.nextLine(), который «съедал» ввод.  

---

# 6. Check.java

1. Исправлено сообщение об ошибке:
   - Добавлен пробел после точки в строке вывода.  

2. Логика валидации:
   - scanner.next() корректно очищает неверный токен.  
   - scanner.hasNextInt() используется правильно.  

---

# 7. City.java

1. Исправлен некорректный комментарий.  
2. Заменена конкатенация строк в цикле на StringBuilder.  
3. Конструктор копирования использует new HashMap<>(paths), что предотвращает изменение исходной карты.  

---

# 8. Name.java

1. Использован StringBuilder в методе toString().  
2. Добавлены проверки на пустые строки.  
3. Корректная расстановка пробелов между частями имени.  
4. Конструкторы оптимизированы через вызов this(...).  

---

# 9. Line.java

1. Добавлен конструктор, принимающий координаты напрямую.  
2. Поля start и end приватные, геттеры оформлены корректно.  
3. Метод toString() возвращает человекочитаемую строку вида:  
   «Линия от {x;y} до {x;y}».  

---
 

Код стал чище, безопаснее, соответствует Google Java Style и удобнее для чтения и сопровождения.

Если нужно, могу подготовить версию отчёта в более строгом академическом стиле или адаптировать под требования преподавателя.
