

## ChatGPT:

## 1.  Использованный промт
Ниже — точный промт, который применялся для получения исправлений и отчёта:

```
Напиши код на с++
```
---

## 2.  Что было исправлено в коде

### 2.1. Проверка корректности ввода
**Проблема:** программа принимала любое число, включая отрицательные и большие значения.  
**Исправление:** добавлена проверка диапазона `1–3`.

### 2.2. Неправильное использование enum
**Проблема:** метод `generatePassword` вызывался через `int`, что нарушало типобезопасность.  
**Исправление:** добавлено приведение `static_cast<PasswordGenerator::Difficulty>(choice)`.

### 2.3. Магические числа
**Проблема:** уровни сложности были представлены числами `1/2/3`.  
**Исправление:** введён enum `Difficulty`.

### 2.4. Плохая архитектура
**Проблема:** вспомогательные функции находились в `main.cpp`.  
**Исправление:** вынесены в приватные методы класса.

### 2.5. Несоблюдение стиля
**Проблема:** имена методов и структура класса были неаккуратными.  
**Исправление:**  
- методы приведены к единому стилю  
- заголовочный файл очищен  
- логика вынесена в `.cpp`  

### 2.6. Улучшение генерации пароля
Использован `std::mt19937` и `std::uniform_int_distribution`, что делает генерацию более надёжной.

---

## 3. Интересные выводы по работе с нейросетью

### 3.1. Нейросеть отлично справляется с рутинным рефакторингом
Она быстро:
- находит стилистические несоответствия  
- предлагает архитектурные улучшения  
- замечает мелкие ошибки, которые легко пропустить  

Это экономит время и снижает когнитивную нагрузку.

### 3.2. Но нейросеть не видит проект целиком
Она анализирует только предоставленный фрагмент.  
Если контекст неполный — возможны ложные предположения.

Вывод: **всегда давать полный код или чётко описывать структуру проекта**.

### 3.3. Нейросеть хорошо объясняет, но не всегда оптимально проектирует
Она предлагает улучшения, но иногда:
- усложняет архитектуру  
- делает избыточные абстракции  
- предлагает решения «по учебнику», а не по ситуации  

Вывод: **нейросеть — помощник, но не архитектор**.

### 3.4. Нейросеть идеально подходит для документации
Она:
- структурирует  
- формулирует  
- делает текст читабельным  
- подстраивается под стиль  

Это особенно полезно для GitHub, отчётов, защиты проектов.

### 3.5. Лучшие результаты достигаются при точных промтах
Если промт расплывчатый — результат будет средним.  
Если промт структурирован — результат почти идеален.

Вывод: **качество промта = качество результата**.

---

## 4. Итог
В ходе работы:
- код был улучшен, очищен и структурирован  
- устранены ошибки и нарушения стиля  
- архитектура приведена к классическому виду  
- создана документация для GitHub  
- протестированы возможности нейросети в рефакторинге и документировании  


---

## GPT:


## 1.  Использованный промт
Для получения отчёта и анализа использовался следующий промт:

```
напиши код на с++
```


---

## 2. 🛠️ Что было исправлено и улучшено

### 2.1. Проверка корректности выбора сложности
**Проблема:** программа принимала любые числа, включая некорректные.  
**Исправление:** добавлена проверка `choice < 1 || choice > 3`.

### 2.2. Устранение глобальных функций
**Проблема:** генерация пароля была реализована как глобальная функция.  
**Исправление:** логика перенесена в метод класса `PasswordGenerator`.

### 2.3. Введение enum вместо чисел
**Проблема:** уровни сложности задавались числами 1/2/3.  
**Исправление:** создан enum `Difficulty { EASY = 1, MEDIUM, HARD }`.

### 2.4. Вынесение логики в отдельные методы
**Проблема:** набор символов и длина пароля задавались прямо в `main`.  
**Исправление:** добавлены методы:
- `getCharacters(Difficulty level)`
- `getLength(Difficulty level)`

Это улучшает читаемость и расширяемость.

### 2.5. Улучшение структуры проекта
**Проблема:** весь код был в одном файле.  
**Исправление:**  
- создан заголовочный файл `Header.h`  
- реализация вынесена в `.cpp`  
- `main.cpp` теперь содержит только пользовательский интерфейс  

### 2.6. Улучшение генерации случайных символов
Используется современный генератор:
- `std::random_device`
- `std::mt19937`
- `std::uniform_int_distribution`

Это делает генерацию более надёжной и предсказуемой.

### 2.7. Приведение кода к единому стилю
- убраны магические строки  
- методы названы единообразно  
- добавлены комментарии `FIX_ME` для фиксации изменений  

---

## 3. Интересные выводы по работе с нейросетью

### 3.1. Нейросеть хорошо справляется с анализом структуры
Она быстро выявляет:
- дублирование логики  
- нарушение принципов инкапсуляции  
- стилистические несоответствия  

Это экономит время при ревью.

### 3.2. Нейросеть помогает формировать документацию
Она:
- структурирует текст  
- формулирует мысли чётко  
- создаёт отчёты, пригодные для GitHub  

Это особенно полезно для учебных проектов и защиты.

### 3.3. Нейросеть не заменяет инженера
Хотя она предлагает улучшения, но:
- не всегда понимает контекст проекта  
- иногда предлагает избыточные абстракции  
- не видит архитектуру целиком  

Вывод: **нужен инженерный контроль**.

### 3.4. Качество ответа зависит от качества промта
Чем точнее запрос, тем лучше результат.  
Если промт расплывчатый — нейросеть выдаёт общий текст.

### 3.5. Нейросеть удобна для рутинных задач
Она отлично подходит для:
- рефакторинга  
- поиска ошибок  
- генерации документации  
- объяснения кода  

Но не для проектирования архитектуры.

---

## 4.  Итог
В ходе работы:
- код был переработан и структурирован  
- устранены архитектурные и стилистические ошибки  
- логика вынесена в класс  
- создана документация о процессе работы  
- протестированы возможности нейросети в инженерных задачах  

## DeepSeek


---

## 1.  Использованный промт
Для получения отчёта использовался следующий запрос:

```
сделай код по заданию на с++
```

---

## 2.  Что было исправлено и улучшено

### 2.1. Инициализация генератора случайных чисел
**Проблема:** seed и генератор находились в `main`.  
**Исправление:**  
- генератор перенесён в конструктор класса  
- используется `std::mt19937` с `std::time(nullptr)`  

Это делает класс самодостаточным.

---

### 2.2. Вынесение логики категорий в отдельный метод
**Проблема:** проверка наличия всех типов символов была встроена в `main`.  
**Исправление:**  
Создан метод:

```
ensureAllCategories(...)
```

Он:
- гарантирует наличие всех категорий (строчные, прописные, цифры, спецсимволы)
- перемешивает пароль
- формирует итоговую строку

Это улучшает безопасность и расширяемость.

---

### 2.3. Разделение уровней сложности на отдельные методы
**Проблема:** логика уровней была захардкожена в `main`.  
**Исправление:**  
Добавлены методы:

- `generateEasy()`
- `generateMedium()`
- `generateHard()`

Каждый метод:
- задаёт длину
- формирует набор символов
- передаёт категории в `ensureAllCategories`

---

### 2.4. Улучшение интерфейса класса
**Проблема:** выбор уровня был через `switch` в `main`.  
**Исправление:**  
Добавлен метод:

```
generateByLevel(int level)
```

Теперь `main` не знает деталей реализации.

---

### 2.5. Вынесение описаний уровней
**Проблема:** текстовые описания были в `main`.  
**Исправление:**  
Добавлен статический метод:

```
getLevelDescription(int level)
```

Теперь UI не содержит логики.

---

### 2.6. Улучшение пользовательского интерфейса
Добавлены:

- ASCII‑разделители  
- вывод состава пароля  
- проверка ввода с очисткой буфера  
- возможность повторной генерации  

Это делает программу удобнее и безопаснее.

---

### 2.7. Улучшение структуры проекта
**Проблема:** весь код был в одном файле.  
**Исправление:**  
- класс вынесен в `Header.h`  
- реализация — в `.cpp`  
- `main.cpp` содержит только UI  

---

## 3.  Интересные выводы по работе с нейросетью

### 3.1. Нейросеть хорошо справляется с анализом больших фрагментов
Она:
- видит архитектурные проблемы  
- предлагает перенос логики в методы  
- замечает дублирование  

Это ускоряет рефакторинг.

---

### 3.2. Нейросеть помогает структурировать код
Она предлагает:
- вынести категории  
- вынести описания уровней  
- разделить ответственность между методами  

Это делает код ближе к SOLID.

---

### 3.3. Нейросеть отлично подходит для документирования
Она:
- формирует отчёты  
- структурирует мысли  
- делает текст читабельным  

Это экономит много времени.

---

### 3.4. Но нейросеть не заменяет инженера
Она:
- не знает контекста проекта  
- иногда предлагает избыточные абстракции  
- не всегда понимает реальные требования  

Вывод: **нужен инженерный контроль**.

---

### 3.5. Качество результата зависит от качества промта
Чем точнее запрос — тем лучше результат.

---

## 4.  Итог
В ходе работы:

- код был переработан и структурирован  
- логика генерации вынесена в отдельные методы  
- добавлена гарантия наличия всех категорий символов  
- улучшен UI  
- создана документация для GitHub  
- протестированы возможности нейросети в рефакторинге и документировании  

Нейросеть показала себя как отличный инструмент для ускорения разработки, но требующий точных инструкций и инженерного надзора.



